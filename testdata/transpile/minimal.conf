filter {

    mutate {
        id => "mutate-the-first"
        add_field => {
            "[boo]" => "[bar]"
        }
    }

    grok {
        
    }

    if [foo][bar] not in ["pippo", "pluto"] {
        # Aggiungere il campo
        mutate {
            add_field => {
                "[foo]" => "pizza"
            }
            # Rename the fields that I do not like
            rename => {
                "[pizza]" => "[fichi]"
            }
            lowercase => ["zizzo", "franco"]
            copy => {
                "[test]" => "[foo]"
            }
        }
    }
}