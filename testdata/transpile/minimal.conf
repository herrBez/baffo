filter {

    mutate {
      capitalize => ["[foo][pirla][chi][legge]"]
      # add_field => {
      #   "[v1]" => "v8"
      # }
      # strip => ["v1"]
      id => "Mutate_0"

      add_field => {
        "[foo]" => "bar"
        "[zizzo]" => "bu"
      }
    }

    mutate {
      convert => {
        "[test]" => "boolean"
      }
      add_field => {
        "[Success]" => "true"
        "[Failure]" => "false"
      }
      id => "Mutate_Convert"
    }

    if [Failure] {
      pippo {
        id => 27
        percentage => 40
      }
    }
    # if [@metadata][pipeline] {
    # # Comment of the Plugin
    # mutate {
    #     id => "Mutate_All"
    #     # comment of gsub
    #     gsub => [
        
    #       # replace all forward slashes with underscore
    #       "fieldname", "/", "_",
    #       # replace backslashes, question marks, hashes, and minuses
    #       # with a dot "."
    #       "fieldname2", "[\\?#-]", "."
    #     ]

    #     join => {
    #       "foo" => ","
    #     }

    #     rename => {
    #       "[syslog][pri]" => "[log][syslog][priority]"
    #       "[another_old_name]" => "[another_new_name]"
    #     }

    #     split => {
    #       "[test]" => ","
    #     }

    #     strip => ["[foo]", "[boo]"]

    #     coerce => {
    #       "[field_null][another_field][perhaps][is][nullv2]" => "boo"
    #     }

        
    #     add_field => {
    #       "[added_field]" => "foo_%{[pizza][puzza]}_%{[pranzo]}"
    #     }
    # }
    # }



}
