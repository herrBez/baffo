input {
    stdin {
        codec => "json"
    }
}
filter {
    # prune {
    #     whitelist_names => ["foo.*", "boo"]
    # }

    syslog_pri {
    }
    syslog_pri {
        syslog_pri_field_name => "pri"
    }
    syslog_pri {
        use_labels => false
        syslog_pri_field_name => "pri"
    }
}